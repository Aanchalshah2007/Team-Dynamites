# Client-side Firebase config (safe to expose -- these are not secrets, unlike
# backend/.env's FIREBASE_PRIVATE_KEY). Copy to .env.local and fill in.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
